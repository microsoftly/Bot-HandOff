import { Session } from 'botbuilder';
import { IRouter } from './IRouter';

export class CustomerMessageRouter implements IRouter {
    //tslint:disable-next-line
    public route(session: Session,  next?: Function): Promise<any> {

    }
}
